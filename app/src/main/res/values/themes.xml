<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Base.Theme.ADI" parent="Theme.Material3.DayNight.NoActionBar">
        <!-- Customize your light theme here. -->
        <!-- <item name="colorPrimary">@color/my_light_primary</item> -->
    </style>

    <style name="Theme.ADI" parent="Base.Theme.ADI" />

    <style name="CustomToolbarStyle">
        <item name="android:background">@color/azul</item> <!-- Color de fondo -->
        <item name="android:titleTextColor">@android:color/white</item> <!-- Color del texto del título -->
        <item name="android:elevation">4dp</item> <!-- Sombra del Toolbar -->
    </style>


    <!-- Estilo para los botones sin bordes redondeados -->
    <style name="ButtonSquare">
        <item name="android:background">@drawable/button_square</item>
    </style>
</resources>